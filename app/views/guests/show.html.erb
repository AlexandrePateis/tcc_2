<p style="color: green"><%= notice %></p>

<%= render @guest %>

<div>
  <%= link_to "Editar", edit_guest_path(@guest), class: "btn btn-sm btn-primary"  %> 
  <%= link_to "Voltar", guests_path, class: "btn btn-sm btn-secondary" %>
  <%= button_to "Apagar", @guest, method: :delete, class: "btn btn-sm btn-danger" %>
</div>
